<!DOCTYPE html>
<html>
<head>
  <title>Shnack</title>
  <link rel="shortcut icon" type="image/ico" href="images/favicon.ico">
  <%= stylesheet_link_tag    "application", :media => "all" %>
  <%= javascript_include_tag "application" %>
  <%= csrf_meta_tags %>
</head>
<body>
<div class="nav"> 
  <%= link_to image_tag("shnack_with_text.png", :class => "logo"), root_path  %>
  <ul>
  <% if user_signed_in? %>
    <li> <%= current_user.email %> </li>
    <li>
      <%= link_to('Logout', destroy_user_session_path, :method => :delete) %>   
    </li>
    <li>   
      <%=link_to "Edit Profile", :controller => "users", :action => "edit", :id => current_user.id %>
    </li>
    <% if current_user.is_super || current_user.is_manager? %>
      <li><%= link_to "Manage", manage_path %></li>
    <% end %>

  <% else %>
    <li>
      Welcome to Shnack
    </li>
    <li>
    <%= link_to('Login', new_user_session_path)  %>  
    </li>
  <% end %>
  </ul>
</div>
<p class="notice"><%= notice %></p>
<p class="alert"><%= alert %></p>


<%= yield %>

</body>
</html>
