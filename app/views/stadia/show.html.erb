<h1> <%= @stadium.name %> </h1>
<h2> <%= link_to "Vendors", stadium_vendors_path(@stadium) %> </h2>
<ul>
	<% @stadium.vendors.each do |vendor| %>
		<li> <%= vendor.name %> </li>
	<% end %>
</ul>

<%= link_to "Back to stadiums list", stadia_path %>