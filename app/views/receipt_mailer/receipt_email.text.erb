Your order #<%= @order.id %> at <%= @order.location_name %> has been placed
=============================================================================

<% @order_items.each do |order_item| %>
   <%= @items[order_item.item_id].name %>  <%= qty %> <%= number_to_currency @items[order_item.item_id].price %> <%= number_to_currency(@items[order_item.item_id].price * qty) %>
<% end %>