
<% order_item.order_modifiers.each do |mod| %>
	<% mod.options.each do |val| %>
		<%= val.name.capitalize %><%= ", " unless val == mod.options.last && mod == order_item.order_modifiers.last %>
	<% end %>
<% end %>