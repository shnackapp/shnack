<h1> <%= @stadium.name %> </h1>


<h2>Select your vendor:</h2>
<ul>
	<% @stadium.vendors.each do |vendor| %>
		<li> <%= link_to vendor.name, new_order_path(:vendor_id  => vendor.id) %> </li>
	<% end %>
</ul>
<br />
<%= link_to "Back to stadiums list", :action => "select_stadium" %>
