<h2 class="headline"> <%= @stadium.name %> </h1>

<div class="list-group">
	<% @stadium.vendors.each do |vendor| %>
		<%= link_to vendor.name, new_order_path(:vendor_id  => vendor.id), :class=>"list-group-item" %> 
	<% end %>
</ul>
<br />
<%= link_to "Back to stadiums list", :action => "select_stadium" %>
