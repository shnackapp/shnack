<h2 class="headline">Select your stadium:</h2>
<div class="list-group">
	<% @stadiums.each do |stadium| %>
		<%= link_to stadium.name, {:action => "select_vendor", :object_id  => stadium.id},:class=>"list-group-item"  %> 
	<% end %>

